# Ruff configuration for FractalWorkspace
# Only enforce rules that catch real bugs — not style or opinions.

target-version = "py310"

[lint]
# F = Pyflakes (undefined names, unused imports, real errors)
# E9 = Syntax errors that prevent execution
select = ["F", "E9"]
